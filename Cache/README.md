# Cache

The **Cache module** dynamically populates the cache with assets at runtime.  
It is designed to keep the repository lightweight and prevent unnecessary Git LFS growth.

## Running

You can run the cache loader using Maven:

```bash
mvn compile exec:java
```
